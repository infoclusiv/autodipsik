# Autodipsik

`sites/newsite/` is intentionally retained as a template/lab module.
The production DeepSeek flow lives in `sites/deepseek/`.

## Cleanup Verification

Run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/verify-cleanup.ps1
```
